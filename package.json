{
  "name": "finance-pro",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:all": "npm install --prefix backend && npm rebuild --prefix backend better-sqlite3 && npm install --prefix frontend && npm run build --prefix frontend"
  }
}