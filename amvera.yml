meta:
  environment: node
  toolchain:
    name: npm
    version: "22"

build:
  skip: no

run:
  command: node --experimental-sqlite --no-warnings src/index.js
  persistenceMount: /data
  containerPort: 3000

workDir: backend