meta:
  environment: node
  toolchain:
    name: npm
    version: "22.12"

build:
  command: npm --prefix frontend install --include=dev && npm --prefix frontend run build

run:
  command: node src/index.js
  persistenceMount: /data
  containerPort: 80