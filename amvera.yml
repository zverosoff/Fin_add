meta:
  environment: node
  toolchain:
    name: npm
    version: 22

build:
  additionalCommands: |
    npm install
    npm rebuild better-sqlite3

run:
  scriptName: src/index.js
  persistenceMount: /data
  containerPort: 80