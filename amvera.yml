meta:
  environment: node
  toolchain:
    name: npm
    version: "22.11"

build:
  skip: no
  additionalCommands:
    - cd frontend && npm install --include=dev && npm run build

run:
  command: node src/index.js
  persistenceMount: /data
  containerPort: 80