meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: |
    npm ci
    npm rebuild better-sqlite3
    cd frontend
    npm ci
    npm run build

run:
  scriptName: src/index.js
  persistenceMount: /data
  containerPort: 80