meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  additionalCommands: npm run build:all

run:
  scriptName: backend/src/index.js
  persistenceMount: /data
  containerPort: 80